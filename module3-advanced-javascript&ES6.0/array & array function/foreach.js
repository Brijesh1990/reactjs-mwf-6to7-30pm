/* foreach : foreach used as function of array in javascript
             foreach is also used as loop
             foreach is used to convert array as values
             
             emp.forEach(callback);


*/

var emp=["brijesh","jayraj","smit","jay","vijay"];
emp.forEach(display);
function display(items)
{
 console.log(items);

}
