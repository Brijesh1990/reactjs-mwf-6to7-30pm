/* map :     map used as function of array in javascript
             map is also used as a loop in react js
             map is used to map each values of array and return in a list of array values
             
             emp.map(callback);


*/

var emp=["brijesh","jayraj","smit","jay","vijay"];
emp.map(display);
function display(items)
{
 console.log(items);

}
